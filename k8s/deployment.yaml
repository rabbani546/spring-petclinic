---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: hello
spec:
  selector:
    matchLabels:
      app: pets
  replicas: 1
  template:
    metadata:
      labels:
        app: pets
    spec:
      containers:
      - name: canp
        image: kanthreg.azurecr.io/sample
        imagePullPolicy: Always
        ports:
        - containerPort: 8020
      imagePullSecrets:
      - name: petsecret
---
apiVersion: v1
kind: Service
metadata:
  name: sepe
  labels:
    app: pets
spec:
  type: LoadBalancer
  ports:
  - port: 8020
    protocol: TCP
    targetPort: 8085
  selector:
    app: pets